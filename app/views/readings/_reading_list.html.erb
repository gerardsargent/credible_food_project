<h1>Readings</h1>

<% if readings.present? %>
  <table>
    <thead>
      <tr>
        <th>Client</th>
        <th>Organisation</th>
        <th>Date collected</th>
        <th>Date observed</th>
        <th>Location</th>
        <th>Plants present</th>
        <th>Plants desired</th>
        <th>Other notes</th>
        <th>Observed by</th>
        <th>Lt ID</th>
        <th>Cust ID</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% readings.each do |reading| %>
        <tr>
          <td><%= reading.client %></td>
          <td><%= reading.organisation %></td>
          <td><%= reading.date_collected %></td>
          <td><%= reading.date_observed %></td>
          <td><%= reading.location %></td>
          <td><%= reading.plants_present %></td>
          <td><%= reading.plants_desired %></td>
          <td><%= reading.other_notes %></td>
          <td><%= reading.observed_by %></td>
          <td><%= reading.lt_id %></td>
          <td><%= reading.cust_id %></td>
          <td><%= link_to 'Show', reading %></td>
          <td><%= link_to 'Edit', edit_reading_path(reading) %></td>
          <td><%= link_to 'Delete', reading, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>Please add your first reading.</p>
<% end %>
<br>

<%= link_to 'New Reading', new_reading_path %>